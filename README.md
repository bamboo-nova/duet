# duet
A plugin that turns Claude Code (cc) and Codex (Sol) into a fixed-role development duo: one plans and reviews, the other verifies and implements. Works in both Claude Code and Codex CLI from a single codebase.
